// Celebration messages for different milestones
window.celebrationMessages = {
    50: {
        english: {
            text: ["Half Century!", "50 Points!", "Amazing Start!"],
            voice: ["Wow! Fifty points!", "Great job! Fifty!", "Half century achieved!"]
        },
        sinhala: {
            text: ["අර්ධ ශතකය!", "ලකුණු 50!", "අපූරු ආරම්භය!", "හරිම ලස්සන!", "පනහ ලකුණු!"],
            voice: ["වාව්! ලකුණු පනහ!", "හොඳටම! පනහ!", "අර්ධ ශතකය සාක්ෂාත් කරගත්තා!", "අපූරු ආරම්භයක්!", "ලකුණු පනහ ලැබුණා!"]
        },
        tamil: {
            text: ["அரைச் சதம்!", "50 புள்ளிகள்!", "அற்புதமான ஆரம்பம்!"],
            voice: ["வாவ்! ஐம்பது புள்ளிகள்!", "சிறந்த வேலை! ஐம்பது!", "அரைச் சதம் எட்டப்பட்டது!"]
        }
    },
    100: {
        english: {
            text: ["Century!", "100 Points!", "You're a Star!"],
            voice: ["Amazing! One hundred points!", "Century completed!", "You are a superstar!"]
        },
        sinhala: {
            text: ["ශතකය!", "ලකුණු 100!", "ඔබ තරුවක්!", "සියය ලකුණු!", "අපූරු දියුණුව!"],
            voice: ["අපූරු! ලකුණු සියය!", "ශතකය සම්පූර්ණ!", "ඔබ සුපිරි තරුවක්!", "ලකුණු සියය සාක්ෂාත් කරගත්තා!", "විශිෂ්ට කාර්ය සාධනය!"]
        },
        tamil: {
            text: ["சதம்!", "100 புள்ளிகள்!", "நீ ஒரு நட்சத்திரம்!"],
            voice: ["அற்புதம்! நூறு புள்ளிகள்!", "சதம் முடிந்தது!", "நீ ஒரு சூப்பர் ஸ்டார்!"]
        }
    },
    200: {
        english: {
            text: ["Double Century!", "200 Points!", "Incredible!"],
            voice: ["Incredible! Two hundred points!", "Double century!", "You are unbelievable!"]
        },
        sinhala: {
            text: ["ද්විත්ව ශතකය!", "ලකුණු 200!", "ඇදහිය නොහැකි!", "දෙසියය ලකුණු!", "විශිෂ්ටයි!"],
            voice: ["ඇදහිය නොහැකි! ලකුණු දෙසියය!", "ද්විත්ව ශතකය!", "ඔබ ඇදහිය නොහැකි!", "ලකුණු දෙසියය සාක්ෂාත් කරගත්තා!", "අතිවිශිෂ්ට දක්ෂතාව!"]
        },
        tamil: {
            text: ["இரட்டைச் சதம்!", "200 புள்ளிகள்!", "நம்பமுடியாதது!"],
            voice: ["நம்பமுடியாதது! இருநூறு புள்ளிகள்!", "இரட்டைச் சதம்!", "நீ நம்பமுடியாதவன்!"]
        }
    },
    300: {
        english: {
            text: ["Triple Century!", "300 Points!", "Unstoppable!"],
            voice: ["Fantastic! Three hundred points!", "Triple century!", "You are unstoppable!"]
        },
        sinhala: {
            text: ["ත්‍රිත්ව ශතකය!", "ලකුණු 300!", "නවත්විය නොහැකි!", "තුන්සියය ලකුණු!", "දිනෙන් දින හොඳ වෙනවා!"],
            voice: ["විශිෂ්ට! ලකුණු තුන්සියය!", "ත්‍රිත්ව ශතකය!", "ඔබ නවත්විය නොහැකි!", "ලකුණු තුන්සියය සාක්ෂාත් කරගත්තා!", "අතිශයින්ම විශිෂ්ටයි!"]
        },
        tamil: {
            text: ["மும்மடிச் சதம்!", "300 புள்ளிகள்!", "தடுக்க முடியாதது!"],
            voice: ["அற்புதம்! முந்நூறு புள்ளிகள்!", "மும்மடிச் சதம்!", "நீ தடுக்க முடியாதவன்!"]
        }
    },
    400: {
        english: {
            text: ["400 Points!", "Fantastic!", "Keep Going!"],
            voice: ["Wonderful! Four hundred points!", "Fantastic score!", "Keep going strong!"]
        },
        sinhala: {
            text: ["ලකුණු 400!", "විශිෂ්ටයි!", "දිගටම කරගෙන යන්න!", "හාරසියය ලකුණු!", "අපූරු දක්ෂතාව!"],
            voice: ["අපූරු! ලකුණු හාරසියය!", "විශිෂ්ට ලකුණු!", "දිගටම ශක්තිමත්ව කරගෙන යන්න!", "ලකුණු හාරසියය සාක්ෂාත් කරගත්තා!", "විශිෂ්ටතම කාර්ය සාධනය!"]
        },
        tamil: {
            text: ["400 புள்ளிகள்!", "அற்புதம்!", "தொடர்ந்து செல்!"],
            voice: ["அற்புதம்! நானூறு புள்ளிகள்!", "அற்புமான ஸ்கோர்!", "தொடர்ந்து முன்னேறு!"]
        }
    }
};

// Messages for red balls only
window.redBallMessages = {
    english: [
        "Careful!",
        "Watch out!",
        "Danger!",
        "Be careful!",
        "Red alert!",
        "Warning!",
        "Oh no!",
        "Uh oh!"
    ],
    sinhala: [
        "ප්‍රවේශම් වන්න!",
        "අනතුරු ඇගවීම!",
        "අන්තරාය!",
        "සැලකිලිමත් වන්න!",
        "රතු ඇඟවීම!",
        "අවදානම!",
        "අපොයි!",
        "අහෝ!"
    ],
    tamil: [
        "கவனம்!",
        "கவனமாக இரு!",
        "ஆபத்து!",
        "கவனமாக இரு!",
        "சிவப்பு எச்சரிக்கை!",
        "எச்சரிக்கை!",
        "ஓ இல்லை!",
        "அய்யோ!"
    ]
};

// System messages
window.systemMessages = {
    gameStart: {
        english: "Help Flippy the Fish!",
        sinhala: "ෆ්ලිප්පී මාළුවාට උදව් කරන්න!",
        tamil: "மீன் ஃபிப்பியை காப்பாற்றுங்கள்!"
    },
    gamePaused: {
        english: "Paused",
        sinhala: "තාවකාලිකව නතර කළා",
        tamil: "நிறுத்தப்பட்டது"
    },
    gameOver: {
        english: "Game Over!",
        sinhala: "ක්‍රීඩාව අවසන්!",
        tamil: "ஆட்டம் முடிந்தது!"
    },
    soundsOn: {
        english: "Sounds On",
        sinhala: "ශබ්ද සක්‍රියයි",
        tamil: "ஒலி இயக்கத்தில் உள்ளது"
    },
    soundsOff: {
        english: "Sounds Off",
        sinhala: "ශබ්ද අක්‍රියයි",
        tamil: "ஒலி அணைக்கப்பட்டுள்ளது"
    },
    voiceOn: {
        english: "Voice On",
        sinhala: "කථනය සක්‍රිය",
        tamil: "பேச்சு இயக்கத்தில் உள்ளது"
    },
    voiceOff: {
        english: "Voice Off",
        sinhala: "කථනය අක්‍රිය",
        tamil: "பேச்சு அணைக்கப்பட்டுள்ளது"
    },
    welcome: {
        english: "Click Start to help Flippy!",
        sinhala: "ආරම්භ කිරීමට ඔබන්න!",
        tamil: "ஃபிப்பியை காப்பாற்ற தொடக்கத்தை கிளிக் செய்யவும்!"
    }
};

// Button text
window.buttonText = {
    startBtn: {
        english: "Start Game",
        sinhala: "ක්‍රීඩාව අරඹන්න",
        tamil: "ஆட்டத்தைத் தொடங்கு"
    },
    pauseBtn: {
        english: "Pause",
        sinhala: "තාවකාලිකව නතර කරන්න",
        tamil: "நிறுத்து"
    },
    resumeBtn: {
        english: "Resume",
        sinhala: "දිගටම කරන්න",
        tamil: "தொடர்"
    },
    restartBtn: {
        english: "Restart",
        sinhala: "යළි අරඹන්න",
        tamil: "மீண்டும் தொடங்கு"
    },
    playAgainBtn: {
        english: "Play Again",
        sinhala: "යළි ක්‍රීඩා කරන්න",
        tamil: "மீண்டும் விளையாடு"
    },
    soundOnBtn: {
        english: "🔊 Sound On",
        sinhala: "🔊 ශබ්ද සක්‍රිය",
        tamil: "🔊 ஒலி ஆன்"
    },
    soundOffBtn: {
        english: "🔇 Sound Off",
        sinhala: "🔇 ශබ්ද අක්‍රිය",
        tamil: "🔇 ஒலி ஆஃப்"
    },
    voiceOnBtn: {
        english: "🎤 Voice On",
        sinhala: "🎤 කථනය සක්‍රිය",
        tamil: "🎤 பேச்சு ஆன்"
    },
    voiceOffBtn: {
        english: "🔇 Voice Off",
        sinhala: "🔇 කථනය අක්‍රිය",
        tamil: "🔇 பேச்சு ஆஃப்"
    }
};

// Function to show a message
window.showMessage = function (text, type, x, y) {
    if (!window.gameState.messagesContainer) return;

    const message = document.createElement('div');
    message.className = `message ${type}`;
    message.textContent = text;

    const offsetX = Math.random() * 100 - 50;
    message.style.left = `${x + offsetX}px`;
    message.style.top = `${y}px`;

    window.gameState.messagesContainer.appendChild(message);

    setTimeout(() => {
        if (message.parentNode) {
            message.parentNode.removeChild(message);
        }
    }, type === 'celebration' ? 3000 : 2000);

    return message;
};

// Update button texts based on current language
window.updateButtonTexts = function () {
    const gs = window.gameState;
    if (!gs.currentLanguage) return;

    // Update body class for language visibility
    document.body.classList.remove('lang-english', 'lang-sinhala', 'lang-tamil');
    document.body.classList.add(`lang-${gs.currentLanguage}`);

    // Update Page Title
    const titles = {
        english: "Fish Ball Collector",
        sinhala: "මාළු බෝල එකතු කරන්නා",
        tamil: "மீன் பந்து சேகரிப்பவர்"
    };
    if (titles[gs.currentLanguage]) {
        document.title = titles[gs.currentLanguage];
    }

    const btns = [
        ['startBtn', 'startBtn'],
        ['pauseBtn', 'pauseBtn'],
        ['restartBtn', 'restartBtn'],
        ['playAgainBtn', 'playAgainBtn'],
        ['soundOnBtn', 'soundOnBtn'],
        ['soundOffBtn', 'soundOffBtn'],
        ['voiceOnBtn', 'voiceOnBtn'],
        ['voiceOffBtn', 'voiceOffBtn']
    ];

    btns.forEach(([id, key]) => {
        const btn = document.getElementById(id);
        if (btn && window.buttonText[key]) {
            btn.textContent = window.buttonText[key][gs.currentLanguage];
        }
    });

    if (gs.gamePaused && document.getElementById('pauseBtn')) {
        document.getElementById('pauseBtn').textContent = window.buttonText.resumeBtn[gs.currentLanguage];
    }
};

// Create celebration particles
window.createParticles = function (x, y, count, type) {
    for (let i = 0; i < count; i++) {
        const particle = document.createElement('div');

        if (type === 'confetti') {
            particle.className = 'confetti';
            particle.style.backgroundColor = `hsl(${Math.random() * 360}, 100%, 60%)`;
            particle.style.left = `${x}px`;
            particle.style.top = `${y}px`;
            particle.style.transform = `rotate(${Math.random() * 360}deg)`;
            particle.style.width = `${Math.random() * 10 + 5}px`;
            particle.style.height = `${Math.random() * 15 + 10}px`;
        } else if (type === 'star') {
            particle.className = 'star';
            particle.innerHTML = '★';
            particle.style.left = `${x}px`;
            particle.style.top = `${y}px`;
            particle.style.fontSize = `${Math.random() * 20 + 15}px`;
            particle.style.color = `hsl(${Math.random() * 60 + 40}, 100%, 50%)`;
        }

        particle.className += ' particle';
        document.body.appendChild(particle);

        const angle = Math.random() * Math.PI * 2;
        const speed = Math.random() * 5 + 2;
        const vx = Math.cos(angle) * speed;
        const vy = Math.sin(angle) * speed - 2;
        const rotation = Math.random() * 10 - 5;
        const gravity = 0.1;
        const life = 100 + Math.random() * 50;

        window.gameState.particles.push({
            element: particle,
            x: x,
            y: y,
            vx: vx,
            vy: vy,
            rotation: rotation,
            gravity: gravity,
            life: life,
            maxLife: life
        });
    }
};

// Update particles
window.updateParticles = function () {
    const gs = window.gameState;
    for (let i = gs.particles.length - 1; i >= 0; i--) {
        const p = gs.particles[i];

        p.x += p.vx;
        p.y += p.vy;
        p.vy += p.gravity;
        p.life--;

        if (p.element.className.includes('confetti')) {
            p.element.style.transform = `rotate(${p.rotation * p.life}deg)`;
        }

        p.element.style.left = `${p.x}px`;
        p.element.style.top = `${p.y}px`;
        p.element.style.opacity = p.life / p.maxLife;

        if (p.life <= 0 || p.y > (gs.canvas?.height || 500) + 100) {
            if (p.element.parentNode) {
                p.element.parentNode.removeChild(p.element);
            }
            gs.particles.splice(i, 1);
        }
    }
};

// Celebration effect for milestones with voice
window.triggerCelebration = function (scoreValue) {
    const gs = window.gameState;
    if (!gs.gameRunning) return;

    // Play celebration sound
    if (gs.soundEnabled && gs.sounds.celebration) {
        gs.sounds.celebration.play();
        setTimeout(() => {
            if (gs.sounds.levelUp) gs.sounds.levelUp.play();
        }, 500);
    }

    // Get celebration messages in current language
    let textMessage, voiceMessage;
    if (window.celebrationMessages[scoreValue]) {
        const textMessages = window.celebrationMessages[scoreValue][gs.currentLanguage].text;
        const voiceMessages = window.celebrationMessages[scoreValue][gs.currentLanguage].voice;

        textMessage = textMessages[Math.floor(Math.random() * textMessages.length)];
        voiceMessage = voiceMessages[Math.floor(Math.random() * voiceMessages.length)];
    } else {
        textMessage = gs.currentLanguage === 'sinhala'
            ? `ලකුණු ${scoreValue}!`
            : gs.currentLanguage === 'tamil'
                ? `புள்ளிகள் ${scoreValue}!`
                : `${scoreValue} Points!`;

        voiceMessage = gs.currentLanguage === 'sinhala'
            ? `ලකුණු ${scoreValue}!`
            : gs.currentLanguage === 'tamil'
                ? `புள்ளிகள் ${scoreValue}!`
                : `${scoreValue} points!`;
    }

    // Show celebration text message
    const canvas = gs.canvas;
    if (canvas) {
        showMessage(textMessage, "celebration", canvas.width / 2, canvas.height / 2);
    }

    // Speak celebration voice message
    if (gs.voiceEnabled && gs.speechSynthesisSupported && window.speakText) {
        setTimeout(() => window.speakText(voiceMessage), 500);
    }

    // Create particles
    if (canvas) {
        createParticles(canvas.width / 2, canvas.height / 2, 100, 'confetti');
        createParticles(canvas.width / 2, canvas.height / 2, 20, 'star');
    }

    // Make the fish happy
    if (gs.fish) {
        gs.fish.happy = true;
        gs.fish.happyTimer = 60;
    }

    // Spawn bonus balls
    for (let i = 0; i < 3; i++) {
        setTimeout(() => {
            if (gs.gameRunning && window.createBallWithQuarterRedFrequency) {
                window.createBallWithQuarterRedFrequency();
            }
        }, i * 200);
    }
};

// Check for milestone celebrations
window.checkForCelebration = function (newScore) {
    const gs = window.gameState;

    // Check for specific milestones
    const milestones = [50, 100, 150, 200, 250, 300, 350, 400];

    for (const milestone of milestones) {
        if (newScore >= milestone && gs.lastCelebrationScore < milestone) {
            window.triggerCelebration(milestone);
            gs.lastCelebrationScore = milestone;
            return;
        }
    }

    // Also celebrate every 50 points after 50 for more frequent celebrations
    if (newScore > 50 && newScore % 50 === 0 && gs.lastCelebrationScore < newScore) {
        window.triggerCelebration(newScore);
        gs.lastCelebrationScore = newScore;
    }
};